import React from 'react';
import styled from 'styled-components/native';

const Box = styled.View`
  padding: 12px;
  border-radius: 10px;
  background-color: #111827;
`;

const Label = styled.Text`
  color: #ffffff;
  font-size: 16px;
  font-weight: 600;
`;

export interface {{pascalCase name}}Props {
  text?: string;
}

export default function {{pascalCase name}}({ text = "{{pascalCase name}}" }: {{pascalCase name}}Props) {
  return (
    <Box>
      <Label>{text}</Label>
    </Box>
  );
}
